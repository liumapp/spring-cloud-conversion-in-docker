spring:
  application:
    name: demo-api-b
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: conversion-config
      profile: dev
      label: master
server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://conversion-eureka:1234/eureka/